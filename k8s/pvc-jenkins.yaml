apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-jenkins
  labels:
    name: pvc-jenkins
spec:
  storageClassName: "standard"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

