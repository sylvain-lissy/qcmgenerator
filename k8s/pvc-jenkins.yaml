apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-jenkins
  labels:
    name: pvc-jenkins
spec:
  storageClassName: "standard"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi

